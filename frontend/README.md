# React Frontend

## Run

1. Install deps:
   - `npm install`
2. Start dev server:
   - `npm run dev`

By default it calls FastAPI at `http://localhost:8000`.

For production, set `VITE_API_URL` (for example in `frontend/.env.production`) to your deployed backend URL.
